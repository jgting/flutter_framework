class RoutePaths {
  static const intro = '/intro';
  static const login = '/login';
  static const home = '/home';
  static const webview = '/webview';
  static const register = 'register'; //子路由不用+'/' (GoRouter 會自動拼接的子路由的前綴)
}
